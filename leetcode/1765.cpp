#include <queue>
#include <cstdio>
#include <cstring>
#include <algorithm>
using namespace std;
using ll = long long;
using uint = unsigned int;
const int inf = 0x3f3f3f3f;

using vvi = vector<vector<int>>;
class Solution
{
public:
    vector<vector<int>> highestPeak(vector<vector<int>> &isWater)
    {
        vvi tans(isWater.size(), vector<int>(isWater[0].size(), 0));
    }
    int bfs(vvi &ans, vvi &isWater, int height = 0)
    {
        int dir[4][2] = {{0, 1}, {1, 0}, {0, -1}, {-1, 0}};
        }
};

int main()
{
    return 0;
}